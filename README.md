# Workers Service

Микросервис для мониторинга публичных Telegram чатов и поиска сообщений о работе на ПВЗ (пунктах выдачи заказов).

## Описание

Workers Service - это автономный REST API сервис, который:

- Мониторит 10-20 публичных Telegram чатов
- Ищет объявления от работников (ищут работу) и работодателей (ищут сотрудников)
- Извлекает структурированные данные из текста (цена, дата, ШК, локация)
- Фильтрует объявления по заданным критериям
- Отправляет уведомления в Telegram бот пользователя
- Хранит результаты в SQLite базе данных

## Технологический стек

- **FastAPI** - REST API фреймворк
- **Pyrogram** - чтение Telegram чатов (MTProto API)
- **python-telegram-bot** - отправка уведомлений (Bot API)
- **SQLite + aiosqlite** - база данных
- **Pydantic** - валидация данных
- **Threading** - фоновые задачи
- **Loguru** - логирование
- **Regex** - извлечение данных из текста

## Установка

### 1. Клонировать репозиторий

```bash
cd /home/alex/PycharmProjects/workers_service
```

### 2. Установить зависимости

```bash
pip install -r requirements.txt
```

### 3. Получить Telegram API credentials

Для работы с Pyrogram необходимо получить `API_ID` и `API_HASH`:

1. Перейти на https://my.telegram.org
2. Войти с вашим номером телефона
3. Перейти в "API development tools"
4. Создать новое приложение
5. Скопировать `api_id` и `api_hash`

### 4. Создать Telegram бота для уведомлений

1. Написать @BotFather в Telegram
2. Создать нового бота командой `/newbot`
3. Скопировать токен бота

## Запуск

```bash
python api.py
```

Сервис запустится на порту **8002**.

Swagger UI доступен по адресу: http://localhost:8002/docs

## API Endpoints

### 1. POST /workers/start - Запуск мониторинга

Запускает задачу мониторинга Telegram чатов.

**Request:**
```json
{
  "user_id": 338908929,
  "mode": "worker",
  "chats": [
    "@pvz_workers_moscow",
    "@wildberries_rabota"
  ],
  "filters": {
    "date_from": "2026-02-10",
    "date_to": "2026-02-20",
    "min_price": 2000,
    "max_price": 5000,
    "shk_filter": "любое"
  },
  "api_id": 12345678,
  "api_hash": "0123456789abcdef0123456789abcdef",
  "notification_bot_token": "BOT_TOKEN",
  "notification_chat_id": 338908929,
  "parse_history_days": 14
}
```

**Response:**
```json
{
  "task_id": "abc-123-def",
  "status": "pending",
  "message": "Мониторинг запущен",
  "started_at": "2026-02-01T15:30:00Z"
}
```

### 2. GET /workers/status/{task_id} - Статус задачи

Получить текущий статус и статистику задачи.

**Response:**
```json
{
  "task_id": "abc-123-def",
  "status": "running",
  "mode": "worker",
  "stats": {
    "total_messages_scanned": 1523,
    "items_found": 47,
    "notifications_sent": 12,
    "last_update": "2026-02-01T16:45:00Z"
  }
}
```

### 3. POST /workers/stop/{task_id} - Остановка мониторинга

Остановить задачу мониторинга.

**Response:**
```json
{
  "task_id": "abc-123-def",
  "status": "stopped",
  "message": "Мониторинг остановлен"
}
```

### 4. GET /workers/list/{task_id}?limit=50 - Список найденных

Получить список найденных объявлений.

**Response:**
```json
{
  "task_id": "abc-123-def",
  "mode": "worker",
  "total": 47,
  "items": [
    {
      "id": 1,
      "author_username": "@ivan_petrov",
      "date": "2026-02-15",
      "price": 3000,
      "shk": "100",
      "message_link": "https://t.me/...",
      "found_at": "2026-02-01T15:35:00Z"
    }
  ]
}
```

### 5. POST /workers/{item_id}/check-blacklist - Проверка ЧС

Отправить объявление на проверку в черный список (заглушка).

**Request:**
```json
{
  "task_id": "abc-123-def"
}
```

**Response:**
```json
{
  "item_id": 1,
  "check_status": "completed",
  "result": {
    "is_blacklisted": false,
    "messages": []
  }
}
```

## Параметры

### Режимы (mode)

- `worker` - поиск работников (ищут работу)
- `employer` - поиск работодателей (ищут сотрудников)

### Фильтры

- `date_from`, `date_to` - диапазон дат работы
- `min_price`, `max_price` - диапазон цен (руб/смена)
- `shk_filter` - фильтр по ШК:
  - `"любое"` - не фильтровать
  - Конкретное значение (например, `"100"`)
  - Качественная оценка (`"мало"`, `"много"`, `"средне"`)

### Извлекаемые данные

- **author_username** - username автора (@ivan_petrov)
- **author_full_name** - полное имя автора
- **date** - дата работы (ISO format)
- **price** - цена за смену (руб)
- **shk** - штрихкоды (если указано)
- **location** - локация (город)
- **message_text** - полный текст сообщения
- **message_link** - ссылка на сообщение
- **chat_name** - имя чата (@pvz_workers)
- **message_date** - дата публикации сообщения

## Формат уведомлений

### Работник:
```
👷 Новый работник!

📅 Дата: 15 февраля 2026
💰 Цена: 3000 руб/смену
📦 ШК: 100
📍 Локация: Мытищи
👤 @ivan_petrov (Иван Петров)
💬 Чат: @pvz_workers_moscow
🔗 https://t.me/pvz_workers_moscow/12345

📝 Полный текст:
"Готов выйти 15 февраля, цена 3000, шк 100, Мытищи"

[Отправить на проверку в ЧС] [Игнорировать]
```

### Работодатель:
```
🏢 Новая вакансия!

📅 Дата: 20 февраля 2026
💰 Оплата: 2800 руб/смену
📦 ШК: 200
📍 Локация: Люберцы
👤 @employer_user
💬 Чат: @pvz_jobs
🔗 https://t.me/pvz_jobs/67890

📝 Полный текст:
"Ищу сотрудника на замену, Люберцы, 20 февраля, 2800, шк 200"

[Связаться] [Игнорировать]
```

## База данных

База данных: `workers.db` (SQLite)

### Таблица tasks

```sql
CREATE TABLE tasks (
    task_id TEXT PRIMARY KEY,
    user_id INTEGER NOT NULL,
    mode TEXT NOT NULL,
    chats TEXT NOT NULL,
    filters TEXT NOT NULL,
    notification_bot_token TEXT NOT NULL,
    notification_chat_id INTEGER NOT NULL,
    status TEXT NOT NULL,
    created_at TEXT NOT NULL,
    stopped_at TEXT
);
```

### Таблица found_items

```sql
CREATE TABLE found_items (
    id INTEGER PRIMARY KEY AUTOINCREMENT,
    task_id TEXT NOT NULL,
    mode TEXT NOT NULL,
    author_username TEXT,
    author_full_name TEXT,
    date TEXT NOT NULL,
    price INTEGER NOT NULL,
    shk TEXT,
    location TEXT,
    message_text TEXT NOT NULL,
    message_link TEXT NOT NULL,
    chat_name TEXT NOT NULL,
    message_date TEXT NOT NULL,
    found_at TEXT NOT NULL,
    notified BOOLEAN DEFAULT 0,
    UNIQUE(message_link)
);
```

## Особенности реализации

### Дедупликация

Объявления дедуплицируются по `message_link`. Индекс `UNIQUE(message_link)` на уровне БД предотвращает повторное сохранение одного и того же сообщения.

### Фоновые задачи

Каждая задача мониторинга выполняется в отдельном потоке (`threading.Thread`). Задачи можно останавливать через `threading.Event()`.

### Извлечение данных

Данные извлекаются из текста с помощью regex паттернов. Поддерживаются:

- Абсолютные даты: "15 февраля", "15.02", "15/02"
- Относительные даты: "сегодня", "завтра", "послезавтра"
- Цены: "3000 руб", "цена 3000", "оплата 2500₽"
- ШК: "шк - 100", "шк мало", "штрихкодов 150"

### Pyrogram vs python-telegram-bot

- **Pyrogram** (MTProto API) - используется для **чтения** публичных чатов
- **python-telegram-bot** (Bot API) - используется для **отправки** уведомлений

## Тестирование

### Запуск тестов извлечения данных

```bash
python message_extractor.py
```

Это запустит тесты на тестовых сообщениях из ТЗ.

### Curl примеры

**Запуск мониторинга:**
```bash
curl -X POST http://localhost:8002/workers/start \
  -H "Content-Type: application/json" \
  -d '{
    "user_id": 338908929,
    "mode": "worker",
    "chats": ["@test_channel"],
    "filters": {
      "date_from": "2026-02-10",
      "date_to": "2026-02-20",
      "min_price": 2000,
      "max_price": 5000,
      "shk_filter": "любое"
    },
    "api_id": 12345678,
    "api_hash": "your_api_hash",
    "notification_bot_token": "your_bot_token",
    "notification_chat_id": 338908929,
    "parse_history_days": 14
  }'
```

**Проверка статуса:**
```bash
curl http://localhost:8002/workers/status/{task_id}
```

**Остановка:**
```bash
curl -X POST http://localhost:8002/workers/stop/{task_id}
```

## Логирование

Логи записываются в:
- Консоль (stdout)
- Файл `workers_service.log` (ротация при 10 MB)

## Важные замечания

1. Это REST API сервис, НЕ Telegram бот
2. Telegram бот (ParserHub) будет создан позже и будет вызывать этот API
3. Endpoint `/workers/{item_id}/check-blacklist` - заглушка (сервис blacklist_service будет создан позже)
4. Для работы требуются Telegram API credentials (api_id и api_hash)
5. Pyrogram создает session файлы (`.session`) для хранения авторизации

## Архитектура

```
workers_service/
├── api.py                 - FastAPI приложение + endpoints
├── models_api.py          - Pydantic модели (Request/Response)
├── models_db.py           - Модели для базы данных
├── parser.py              - Парсинг Telegram чатов (Pyrogram)
├── message_extractor.py   - Извлечение данных из текста (regex)
├── filters.py             - Фильтрация объявлений
├── db_service.py          - Работа с SQLite
├── tg_notifier.py         - Отправка уведомлений в Telegram
├── state_manager.py       - Управление состоянием задач
├── tasks.py               - Фоновые задачи (threading)
├── requirements.txt       - Зависимости
└── README.md              - Документация
```

## Лицензия

Proprietary

## Контакты

GitHub Issues: https://github.com/yourusername/workers_service/issues



